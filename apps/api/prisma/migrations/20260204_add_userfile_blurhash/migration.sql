-- This migration was applied directly to the database without being tracked in Prisma
-- It adds the blurhash column to UserFile table for image optimization
-- Since it's already applied, this is a placeholder to sync the migration history

-- Verify the column exists (it should already be there from the database)
-- The UserFile model in schema.prisma already references this blurhash field
