/**
 * 🔐 Auth Provider Re-export
 * 
 * Re-exports auth context from providers directory
 */

export { AuthProvider, useAuth, useAuth as useAuthContext, useUser, useIsAuthenticated } from '@/providers/auth-provider';
